export const aiDomainPriorities = `**no preset emphasis** — surface meaningful events from every domain class at equal weight. let the live data dictate what's worth narrating, not a preset bias.`;

export const aiWatchedRsns = `**none** — no member is pre-elevated for tighter narration. all clannies tracked at equal cadence.`;

export const aiTopicAvoids = `**none** — no topic is pre-suppressed. every event class is fair game.`;
