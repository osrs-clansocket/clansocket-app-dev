import { primitive } from "../../core/index.js";

const container = primitive("div", "container");
const div = primitive("div");
const header = primitive("header");
const footer = primitive("footer");
const nav = primitive("nav");
const article = primitive("article");
const asideEl = primitive("aside");
const mainEl = primitive("main");

export { container, div, header, footer, nav, article, asideEl, mainEl };
